# Dashboard

The dashboard is the main SpilledCinema UI.

## Notes

- This app is deployed separately when you want the web version.
- The Electron desktop app can point at this dashboard locally.
- The Chrome extension can bridge this dashboard to a local runtime.

## Commands

- Dev:
  `npm run dev -w apps/dashboard`
- Build:
  `npm run build -w apps/dashboard`
