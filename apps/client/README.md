# Client Apps

- `extension`
  Chrome bridge that lets the web dashboard talk to a local node/runtime without a public node URL.
- `native`
  Electron desktop app that runs the dashboard and local node on the same machine.

## Workflows

- Build the extension:
  `npm run build -w @spilledcinema/client-extension`
- Run the desktop app:
  `npm run dev:native`
